# This is a template file for the settings_secret.py used when running via Docker.
# It is not used directly - copy it to oppiamobile/settings_secret_docker.py and
# fill in real values there. settings_secret_docker.py is bind-mounted into the
# container as oppiamobile/settings_secret.py at runtime (see docker-compose.yml),
# overriding oppiamobile/settings_base.py. Values are supplied as environment
# variables via docker-compose.yml (sourced from .env).

import os

# SECURITY WARNING: keep the secret key used in production secret!
# Read about SECRET_KEY: https://docs.djangoproject.com/en/dev/topics/signing/
SECRET_KEY = os.environ.get('SECRET_KEY', 'CHANGE_ME_SET_SECRET_KEY_ENV_VAR')

# Database
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': os.environ.get('DB_NAME', 'oppia'),
        'USER': os.environ.get('DB_USER', 'oppiauser'),
        'PASSWORD': os.environ.get('DB_PASSWORD', ''),
        'HOST': os.environ.get('DB_HOST', 'db'),  # 'db' matches the docker-compose service name
        'PORT': os.environ.get('DB_PORT', '3306'),
        'OPTIONS': {
             'charset': 'utf8mb4'
            },
        'TEST': {
            'CHARSET': "utf8mb4",
            'COLLATION': "utf8mb4_unicode_ci",
        }
    }
}

# List of allowed hostnames
ALLOWED_HOSTS = os.environ.get('ALLOWED_HOSTS', 'localhost,127.0.0.1').split(',')
BASESITE_URL = os.environ.get('BASESITE_URL', 'YOUR_MAIN_URL')

# Caddy terminates TLS and proxies to gunicorn over plain HTTP - without this,
# Django thinks every request is insecure and rejects cross-scheme CSRF checks.
SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')
CSRF_TRUSTED_ORIGINS = [BASESITE_URL]

# Debug mode on/off - display detail error pages
# if DEBUG is False, you also need to properly set the ALLOWED_HOSTS setting.
# Failing to do so will result in all requests being returned as "Bad Request (400)".
DEBUG = os.environ.get('DEBUG', 'True') == 'True'

# Android app PackageId - for Google Play link and opening activities from digest
OPPIA_ANDROID_ON_GOOGLE_PLAY = os.environ.get('OPPIA_ANDROID_ON_GOOGLE_PLAY', 'True') == 'True'
OPPIA_ANDROID_DEFAULT_PACKAGEID = os.environ.get('OPPIA_ANDROID_DEFAULT_PACKAGEID', 'org.digitalcampus.mobile.learning')
OPPIA_ANDROID_PACKAGEID = os.environ.get('OPPIA_ANDROID_PACKAGEID', 'org.digitalcampus.mobile.learning')

TIME_ZONE = os.environ.get('TIME_ZONE', 'UTC')

SERVER_EMAIL = os.environ.get('SERVER_EMAIL', 'adming@email.org')

OPPIA_GOOGLE_ANALYTICS_ENABLED = os.environ.get('OPPIA_GOOGLE_ANALYTICS_ENABLED', 'False') == 'True'
OPPIA_GOOGLE_ANALYTICS_CODE = os.environ.get('OPPIA_GOOGLE_ANALYTICS_CODE', 'YOUR_GOOGLE_ANALYTICS_CODE')
OPPIA_GOOGLE_ANALYTICS_DOMAIN = os.environ.get('OPPIA_GOOGLE_ANALYTICS_DOMAIN', 'YOUR_DOMAIN')

OPPIA_MAX_UPLOAD_SIZE = int(os.environ.get('OPPIA_MAX_UPLOAD_SIZE', 5242880))  # max course file upload size - in bytes

# turns on/off ability for users to self register
OPPIA_ALLOW_SELF_REGISTRATION = os.environ.get('OPPIA_ALLOW_SELF_REGISTRATION', 'True') == 'True'

OPPIA_ALLOW_PROFILE_EDITING = os.environ.get('OPPIA_ALLOW_PROFILE_EDITING', 'True') == 'True'

OPPIA_SHOW_GRAVATARS = os.environ.get('OPPIA_SHOW_GRAVATARS', 'True') == 'True'

# determines if the points system is enabled
OPPIA_POINTS_ENABLED = os.environ.get('OPPIA_POINTS_ENABLED', 'True') == 'True'

# determines if the badges system is enabled
OPPIA_BADGES_ENABLED = os.environ.get('OPPIA_BADGES_ENABLED', 'True') == 'True'


BADGE_AWARDING_METHOD = os.environ.get('BADGE_AWARDING_METHOD', 'all_activities')